"""Real transistor-level layout for the ``lock_detector`` block (issue #296).

See ``layout/evidence/lock-detector-layout/PROOF.md`` for the DRC-clean
evidence record and ``build.py`` for the entry point that assembles the GDS
(``python3 -m layout.pll_top.lock_detector.build --which lock_detector``).
"""

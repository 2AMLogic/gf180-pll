# sim/ — aggregated characterization report

**Purpose.** `sim/README.md` documents each campaign's *methodology* and
migration status; `spec/pll.md`'s summary table states each parameter's
*target* and its overall status (measured / derived / budget / waived). Until
now, nothing pulled every campaign's *headline result* into one place with a
citation a reader can check without opening every campaign directory in turn.
This report is that aggregation: one row per `sim/*/records/` campaign
directory, each citing its own current evidence record(s) by path and content
hash.

This report does not create new evidence, re-derive any number, or relax
`spec/pll.md`. It transcribes what is already committed under `sim/` and
cross-references it against `spec/pll.md`'s own "Summary table" (line 66) and
"Verification owed" table (line 879) so the two documents cannot silently
disagree about what is and is not substantiated. Where they might read as
disagreeing, that is called out explicitly below rather than resolved in
either direction.

Filed against #147 (part of #127, the T1/bronze gap tracker), per #142/#143's
finding that no such aggregated artifact existed as of this repository's
2026-08-16 T1 re-read.

## How to read this report

- **Coverage.** One entry below for every directory under `sim/` that
  contains a `records/` subdirectory. At this report's original writing that
  was **20 campaign directories, 61 evidence records**; the tree has grown
  since, and the current count is **25 campaign directories, 93 evidence
  records**. Both figures are derived, not remembered: the records are
  `find sim -path '*/records/*.md' -type f | wc -l`, and the campaigns are the
  distinct `sim/<name>/` prefixes of those same paths — the enumeration
  `sim/lib/record-campaigns.sh` exports and both sim-side CI checks share.
  (An earlier revision of this bullet offered `find sim -maxdepth 1 -type d`
  for the campaign count; that counts every directory under `sim/`, including
  `harness/`, `lib/`, `tests/` and any campaign whose `records/` is still
  empty, so it never produced the number stated next to it.) `README.md`'s
  status section states the same pair as **93 evidence records across 25
  verification campaigns**; `sim/lib/check-readme-status.sh` grades that line
  against the tree on every CI run, and since #237's aggregate-count rule
  `sim/lib/check-characterization-coverage.sh` grades *this* bullet the same
  way. It needed to: for six weeks this bullet carried the counts of a much
  smaller tree — 19 records and 3 campaigns behind — *and* asserted that the
  graded README line agreed with those figures, which it had not for as long.
  Quoting a checked document is not the same as being checked, so a count
  stated here is now a build failure when it drifts rather than a sentence
  someone is expected to re-read.
- **"Latest record."** Record IDs are `<YYYYMMDD>-<HHMMSS>-<sha>`
  (`sim/README.md`'s naming convention), so a lexicographic sort of
  `records/*.md` gives chronological order for free. Unless noted otherwise
  under a campaign's own entry, "latest" below means the chronologically
  last record in that directory. **This is not always the same as "the
  current, citable record for a given claim"** — several campaigns mint more
  than one live record per directory (a DC bench and a switching bench, a
  raw measurement and a later re-classification, a full-grid record and a
  narrow single-corner diagnostic). Where that applies, the entry cites
  every record actually needed to state the headline result honestly and
  says so.
- **Content hash.** `sha256sum` of the record file itself (first 12 hex
  characters shown; recompute the full digest with
  `shasum -a 256 <path>` or `sha256sum <path>`). This is **not** the
  record's own `<record-id>` git-sha suffix (that names the repo commit the
  run was taken against) — it is a hash of the Markdown file's bytes,
  included so drift or tampering in a nominally append-only file is
  mechanically detectable. Records under `sim/` are never edited after
  creation (`sim/README.md`'s append-only rule), so a hash mismatch against
  this report at any later date means either this report has gone stale
  (the likely case — see "Maintenance" below) or the record's append-only
  guarantee was violated (worth investigating).
- **Status column.** Uses `spec/pll.md`'s own vocabulary where a campaign
  substantiates a numbered spec row (**measured** / **derived** / **budget**
  / **waived**), and a plain description for campaigns whose claim is a
  design-input decision rather than a spec line (`spec/pll.md`'s own
  "design-input claim" form — no ratified spec anchor, referenced by issue
  number instead).
- **What this report is not.** It does not restate every measured number —
  each campaign's record holds the full per-corner tables, raw logs, and
  methodology; this report exists to point at them, not duplicate them. Read
  the cited record for anything beyond the headline.

## Summary table

| Campaign | Latest record (path, sha256 prefix) | Headline result | Spec / issue reference | Status |
|---|---|---|---|---|
| `devchar-delay` | `sim/devchar-delay/records/20260801-084448-01b0db8.md` (`987bc2e2a231`) | Per-PVT-corner current-starved delay-cell drive/delay and ring frequency vs. starving current, 45-point grid. Overall PASS. | #4 → #8 (design-input; feeds VCO stage sizing, no spec anchor of its own) | design-input, measured |
| `devchar-cp` | `sim/devchar-cp/records/20260801-102353-79f398e.md` (`ab2c61a7368c`) | Output resistance and compliance-knee ladder for 6 candidate charge-pump mirror stacks, 45-point grid. Overall PASS. Superseded predecessor `20260731-041340-833292f` still carries one column family (`vcomp_ro50`) and a decimated-curve artifact this migrated record does not reproduce. | #4 → #9 (design-input; feeds CP mirror-stack choice, no spec anchor of its own) | design-input, measured |
| `devchar-passives` | `sim/devchar-passives/records/20260801-102454-79f398e.md` (`a3334ef1c371`) | MIM-vs-MOS-cap C–V linearity, density, and leakage comparison, full passive-corner grid. Overall PASS — feeds the loop-filter cap-type decision (`spec/pll.md#area`). **Off the mandated grid in both directions at once**, as the record's own corner field declares: 9 points across 3 bundles (`typical`/`all-fast`/`all-slow` × 3 temperatures) at 3.30 V only — the passive axes are swept to their `ff`/`ss` extremes, which the mandated grid never reaches, while the five MOS bundles and the ±10 % supply axis are not swept at all, because the DUT is ten capacitors with no active device and its own 0 → 3.63 V C–V ramp already spans the supply range. | #4 → #10 (design-input; feeds loop-filter cap choice) | design-input, measured |
| `divider-ratio` *(pre-harness, superseded runner — see caveat below)* | `sim/divider-ratio/records/20260731-171817-0a12e6c.md` (`1de4c63bc587`) | 235-point chain-ratio sweep: 0 of 235 points measured a ratio other than N; 61 distinct N values exercised at 200 MHz. Retiming setup closure at N=64/200 MHz: worst margin **3.03e-10 s (6.1 % of a VCO period)** at `ss`/125 °C/2.97 V — **this is the number `spec/pll.md`'s summary table row 3 cites.** | `spec/pll.md#multiplication-ratio` (row 3); #11 → DR-001 Decision 3 | **measured** |
| `divider-ratio-dff` | `sim/divider-ratio-dff/records/20260801-125114-3f883e3.md` (`135fe22b5a86`) | `dff_tg_3v3` setup/hold/clk→Q across the full PVT grid — the retiming flop's timing-budget input joined into `divider-ratio-chain`'s margin table. Overall PASS. | #11 (design-input; migrated sibling of `divider-ratio`) | design-input, measured |
| `divider-ratio-cell` | `sim/divider-ratio-cell/records/20260801-140529-3f883e3.md` (`0e821b673dc6`) | Single `div23_cell` moduli (÷2/÷3) on both output edges and both input duty cycles, top/bottom of the band, full PVT grid. Overall PASS at every point. | #11 (design-input; migrated sibling of `divider-ratio`) | design-input, measured |
| `divider-ratio-chain` | `sim/divider-ratio-chain/records/20260802-100727-082c879.md` (`2c7911871d5f`) | Full 6-cell chain ratio + retiming setup closure, migrated onto `sim/harness`. Worst setup margin **3.0283e-10 s (6.1 %)** at `ss`/125 °C/2.97 V — reproduces the pre-migration `divider-ratio` number above; worst hold margin 2.00e-09 s. 0 of 17 evaluated points closed with negative margin. | `spec/pll.md#multiplication-ratio` (row 3, reproduces it); #11 → DR-001 Decision 3 | **measured** (migrated) |
| `lock-detector` | `sim/lock-detector/records/20260919-002812-1b12179.md` (`247ecf875bc8`) | Window-comparator assert/deassert behavior at every PVT corner, **205 points, clean tree** — the **trimmed** `delaywin_3v3` (DR-014, #411) with each corner bundle at the code the [Lock-detector window trim-code rule](../spec/pll.md#lock-detector-window-trim-code-rule) selects for it (c3 `ss`/`all-slow`, c6 `fs`, c7 `typical`/`sf` + the six passive-only bundles, c11 `ff`/`all-fast`). Those thirteen bundles are a **13-bundle, 117-point PVT grid**, a superset of the mandated 45 — and one that moves nothing here: the eight added bundles re-skew only the passive `.lib` sections, and all 72 of their points reproduce their MOS twin's `twin_r`, `twin_f` and verdict to the digit in `raw_measures.csv`, so every number below binds at a mandated-grid bundle. Supersedes `20260916-122705-98c935b.md`, which measured the untrimmed W = 9.5 µm cell over the same claim. **T1′ and T2′ are both met, the first time T2′ has been**: window edges **1.14 / 1.16 ns** at `fs`/−40 °C/3.63 V (T1′, **+14 … +16 %** above the 1 ns lower edge) and **1.78 / 1.80 ns** at `ss`/125 °C/2.97 V (T2′, **10.0 … 11.0 % below** the 2 ns upper edge), each resolved to 0.02 ns at the corner it binds at — against the untrimmed cell's [2.02, 2.04) ns, 1.0–2.0 % past budget. **Observable PVT spread 1.53 … 1.58×** against DR-013 Decision 4's ≤ 1.65× and 1.91–1.96× untrimmed; ≤ **1.62×** as a bound that does not depend on the ladder's resolution anywhere but those two corners (`WIDE = ERR · ERRD` is empty below `t_win`, so the observable is never smaller than `t_win`, whose minimum over all 205 points is 1.1084 ns). **This is the number `spec/pll.md`'s summary table row 16 cites.** Behaviour is unchanged by the trim: 0 of 205 points fail the four-check acceptance, 0 false asserts on a large static phase error or a frequency error, worst deassert latency 5.63 ns (`sf`/125 °C/2.97 V), assert-from-cold 0.680 … 1.913 µs, `t_win` 1.108 … 1.740 ns. **T1′ binds at a different bundle than it did untrimmed** — `fs`, not `ff` — because the rule pulls every bundle toward the same reference-condition target, so which one is narrowest at its own PVT extreme becomes a measured question. **Still owed**: a 0.02 ns refinement at `ff`/−40 °C/3.63 V and `ss`/−40 °C/3.63 V (coarse ladder only there, edges bounded to [1.0, 1.2) ns), T4/T5 below 25 MHz, and extraction + mismatch (#18). **`sim/supply-sensitivity` at the new window is now partly discharged** (#417, `20260920-180604-0f91a9b`): DR-013's window-vs-offset crossing is measured in one closed loop and **confirmed** at both cells DR-013 names — `typical`/−40 °C/3.63 V asserts against a settled 0.8135 ns offset, `ff`/27 °C/3.63 V does not against 1.233 ns — but that is a declared 2-cell subset, so a trimmed-window **full-grid** run of that campaign is **#437**. | `spec/pll.md#lock-detector` (row 16), `spec/pll.md#lock-detector-window-trim-code-rule`; #11 → DR-002 Decision 4; #393 → #401 → DR-013 → #407 → DR-014 → **#411** | **measured** (205 points); targets **met** (T1′/T2′), conditional on the trim rule; T4/T5 still uncharacterized |
| `lock-window-sizing` | `sim/lock-window-sizing/records/20260915-202802-79c0cee.md` (`c0e40498bac3`) | 10 `delaywin_3v3` MOS-cap sizings × the full 13-bundle, 117-point PVT grid — 1170 points, all measured. The campaign's control (the committed cell as drawn) reproduces `lock-detector`'s own window at both binding corners to within 0.3 % / 0.9 %, tying the ladder's baseline to committed evidence. **W = 9.5 µm is the only sizing holding the window inside DR-010's `1 … 2 ns` band at every corner simultaneously** (1.017 … 1.956 ns, +1.7 % / +2.2 % of margin), bracketed on both sides by W = 9 µm (below) and W = 10 µm (above) — but see `lock-detector`'s row above: this idealized-stimulus margin did not survive the full-loop, realistic-edge-rate re-characterization at the same T2′-binding corner (#401). **DR-013 Decision 1 makes that structural**: this campaign measures `t_win`, T1′/T2′ bound the flag's observable, and the two differ by δ — so the ladder remains the sizing coordinate but is no longer sufficient evidence for a T1′/T2′ verdict on its own. The retired T1 (≥ 2.5 ns everywhere) is first met at **W = 26 µm**, where the window reaches **4.924 ns** — the flag would assert on a part 4.9× outside the 1 ns criterion it observes. The window's PVT spread falls only 1.928× → 1.906× across that 3.25× sizing range, and does not move at all when the eight passive bundles are added: geometry re-centres the window, it does not narrow it. Its own 0.5 µm ladder step (4.6–4.8 % of window) is the trim-step granularity DR-014 sized its quantization margin against — and the trimmed cell #411 built lands a **2.7–4.7 %** step instead, so this ladder is the coordinate the trim's own step was chosen relative to rather than the trim's own evidence (that is `lock-window-trim`). **This campaign is now historical for the T1′/T2′ verdict**: the DUT it characterises — a single fixed MOS-cap sizing — no longer exists in the design, which carries the trimmed cell. It is kept as the measurement that established the spread-is-a-property-of-the-topology finding DR-013 Decision 4 rests on. **Schematic-level, no mismatch** — see the record's own limitations. | `spec/pll.md#lock-detector` (row 16); #387 → DR-010; implemented by #393, T2′ margin question decided by DR-013 (#401), sized by #407, mechanism picked by DR-014, implemented and re-characterized by #411 | **measured** (1170 points); **superseded as the design's sizing basis** by the trim (#411), and retained as the evidence that no sizing of that topology could meet DR-013 Decision 4 |
| `lock-window-trim` | `sim/lock-window-trim/records/20260917-185928-8adff3d.md` (`5284862db783`) | The **trimmed** `delaywin_3v3`'s code-to-window map and the code table the [Lock-detector window trim-code rule](../spec/pll.md#lock-detector-window-trim-code-rule) is built from — 16 trim codes × the full 13-bundle, 117-point PVT grid, **1872 points, all measured, clean tree**. The successor axis to `lock-window-sizing`'s geometry ladder, and deliberately a separate campaign rather than a second axis on it: the DUT is a different circuit (DR-014's trimmed cell, #411), and the swept quantity is a **real pin on the committed cell** rather than a parameterised replica's parameter — so there is no replica-vs-control agreement column to prove, and what is swept here is exactly what `lock-detector` instantiates. **The code-to-window map is monotonic at every corner** (0 of 195 adjacent steps non-monotonic) — the property the cell's weight-scaled pass gates buy and an unweighted switch would break, checked per corner rather than asserted. Step **2.7–4.7 %** of window, full range **1.69×** against a **1.30×** fastest-to-slowest reference-condition spread, so the range is centred with headroom at both ends (3/12 codes spare at `ss`, 11/4 at `ff`). **With the rule applied every bundle is in band at every one of its own PVT points**: **1.106 … 1.726 ns** against the ratified [1, 2] ns band (+10.6 % / +13.7 % at the binding edges), grid spread **1.561×**, and **1.610×** for a continuous process population — the pessimistic reading that adds one whole trim step of un-applied correction on top of the worst within-bundle spread, and the number to compare against DR-013 Decision 4's **≤ 1.65×**. **No single fixed code substitutes for the rule**: the one-code spread is 1.977–1.988× at *every* one of the 16 codes, and the two closest both miss the band (code 6 falls to 0.9746 ns at `all-fast`/−40 °C/3.63 V, code 7 reaches 2.0029 ns at `ss`/125 °C/2.97 V). This measures the **bare chain** — the quantity the code moves and a tester can measure; the **observable** window DR-013 Decision 4's target is stated against is measured through the assembled loop in the `lock-detector` row above, at the codes this record selects. **Schematic-level, no mismatch** — a segment-to-segment mismatch inside one stage's binary array would appear as trim DNL and is not characterised here; #18 must re-take this before the code table is final. | `spec/pll.md#lock-detector-window-trim-code-rule`; `spec/pll.md#lock-detector` (row 16); DR-013 Decision 4/6 → DR-014 → **#411** | **measured** (1872 points); spread target **met** (1.610× continuous against ≤ 1.65×) |
| `lock-window-proxy` | `sim/lock-window-proxy/records/20260925-050022-6d57802.md` (`bdb84dc32ec8`) | **Can a tester execute the trim rule from the pads? No — measured, not assumed.** The four candidates `pll_top`'s pad list offers, each paired at every one of the full 13-bundle, 117-point PVT grid's points with the committed `delaywin_3v3`'s own **sixteen-code** window map measured in the same run, each calibrated at **one** bundle (`typical`) and scored at the other twelve, against a **no-measurement control** (every part left at the power-up code 8). The window half of the pairing is validated rather than assumed: 1872 shared points against `lock-window-trim`'s single-copy campaign agree to **0.999988 … 1.00001**, geometric mean 0.999999. **The free-running ring period read at `CLK` is worse than not trimming at all** — 2.434× window spread against the untrimmed control's 1.984×, out of the ratified band at both edges, worst code error 8 of 16 — and the campaign measures *why*, in two independent mechanisms a MOS-only sweep could not have shown: `t_win` is invariant to all six passive-only bundles to four significant figures (every device in `delaywin_3v3` is an `nfet_03v3`/`pfet_03v3`) while the ring is starved through three `ppolyf_u_3k` bias resistors (DR-009), so `res_ff`/`res_ss` move its period **∓27 %** against a window that does not move at all; and across the MOS axis its sign is **inverted**, the ring running 6 % faster at `ss` where the window is 16 % slower. Both ring operating points (band 4 / 1.80 V and band 7 / 2.70 V) behave identically, so this is the topology, not the read point. **The two pad-to-pad digital skews are far better and still insufficient**: `CLK`→`DIVOUT` leaves a **1.132×** residual on the window's 1.316× process spread and holds the ratified [1, 2] ns band at all 117 points, but lands at **1.697×** grid spread (1.74–1.78× once one trim step of rounding is added for a continuous population) against DR-013 Decision 4's **≤ 1.65×**, and its worst code error of **+4** is one code outside the +3 bound DR-022 Tier A derives — every error it makes is positive and the +3/+4 bundles are the fast ones with the most upward headroom, so the band survives on where this grid's thirteen bundles sit rather than on a property of the selector. `CLK`→`FB` is worse (1.735×), sunk by a single outlier: the transmission-gate flop's clock-to-Q at `ff` is 0.557× typical against a window at 0.882×. **Limitations** — schematic-level on both sides of every mapping, no mismatch, thirteen bundles are not a process distribution, and the skews are measured between **on-die** nodes while a tester measures pad to pad (`design/` carries no output pad driver for `FB`, `DIVOUT` or `LOCK` at all). Unaffected by **#515**: the window phase instantiates `delaywin_3v3` directly and drives all four trim pins itself. | `spec/pll.md#lock-detector-window-trim-code-rule` (the Executing-the-rule-at-test subsection); `spec/pll.md#lock-detector` (row 16); **DR-022** ← **#501** | **measured** (117 points, 234 decks, clean tree); the question is **answered in the negative** — no present pad-referred quantity may be written into `spec/` as the rule's execution route |
| `loop-dynamics` | `sim/loop-dynamics/records/20260731-202550-82af5a9.md` (`004d3ad7c9ad`) | Loop-filter component extraction, area (C1 = 0.0303 mm², 20.2 % of the 0.15 mm² budget), the Icp trim-code rule (`spec/pll.md`'s normative "Icp trim-code rule"), and small-signal lock-time estimates (71 µs at f_ref = 1 MHz, structural floor 43 µs — **the number `spec/pll.md`'s summary table row 9 cites**). **Overall: PASS on the contracted operating space, FAIL off it** (i.e. only when the Icp trim-code rule is followed). | `spec/pll.md#loop-bandwidth`, `#phase-margin`, `#lock-time` (rows 8/8a/9), `#area`; #4/#8/#9 → #10 | **measured**, conditional PASS |
| `mc-cp-mismatch` | `sim/mc-cp-mismatch/records/20260923-095854-1655e11.md` (`bcb946f651e9`) — **supersedes** `20260817-135712-0e9cfc9.md` (`873f8454df68`) for this claim at raised sample counts; the nominal-only `sim/mc-cp-mismatch/records/20260731-212614-640560e.md` (`b4a7026934b3`) stands as historical evidence, not superseded. | Monte Carlo mismatch-only dispersion COMBINED with a 3-point PVT corner subset (`typical/27C/3.30V`, `ss/125C/2.97V`, `ff/-40C/3.63V` — see that record's Corner-matrix field for the reduced-grid justification), closing the T1/bronze checklist gap (#127 item 6) the nominal-only record left open. At n = 100/corner (`N_DC`, raised by #482) **term 1 measures 13.2172 % at `ff/-40C/3.63V`** — 4.4 standard errors past the ±12 % budget in force when the record was minted, where the superseded n = 20/corner record read 11.72 % and passed on a margin smaller than its own sampling noise. **That exceedance is resolved by DR-018 (#483), not by the measurement**: the budget was re-derived from the ratified ≤ −55 dBc reference-spur line and widened to **±20 %**, which the same 13.2172 % fits at 66 % utilisation. The record keeps its FAIL verdict and its bytes (append-only); the campaign runner now checks against ±20 %. **#487 then corrected the statistic that 13.2172 % is**: it is `mean(\|x\|) + 3·sd(\|x\|)` on samples folded to their absolute value, which the record and the budget table both labelled `\|mean\| + 3σ` / "3σ". On the same committed 300 samples the signed `\|mean\| + 3σ` — the statistic the budget column actually describes, and the one the runner now reports and takes the verdict on — is **17.4798 %** (87.4 % of ±20 %, still a PASS; DR-018 priced both readings). Re-derive either from the committed CSVs with `sim/mc-cp-mismatch/testbench/run.sh --restat 20260923-095854-1655e11`. Terms 2/2a, 3 and 4 were re-checked for the same folding defect, never had it, and pass as measured: 275.4 ps against ±3 ns, 4.25 fC against ±20 fC, 864.4 ps against ±3 ns (0.871 ns including the divider-retiming flop's clk→Q mismatch 3σ). Feeds the reference-spur derivation (`spec/pll.md#reference-spur`, "Statistical residual net charge" step — DR-018 refreshes that derivation to −56.6 dBc with these corner-combined terms folded in). | #15, #146, #482 → **#483 → DR-018**; feeds `spec/pll.md#reference-spur` (row 7, derived) | design-input / distribution claim, measured; term 1's budget **amended** by DR-018 rather than met as originally stated |
| `reference-spur` | `sim/reference-spur/records/20260816-132150-5f405e7.md` (`f99061a2855f`) | First **direct** closed-loop spur measurement in this repository: the ±f_ref sidebands read out of the locked `pll_top` output spectrum at 5 spanning PVT corners (all five MOS bundles, all three temperatures, all three supplies), f_ref = 25 MHz / N = 6 / f_out = 150 MHz / band 6 / Icp code 0. Measured **−57.0 dBc worst** (`sf`/−40 °C/2.97 V) to −72.7 dBc best (`fs`/125 °C/3.63 V) — **PASS against the −55 dBc target at the measured 150 MHz at every corner**; scaled by +2.50 dB to the ratified band's binding 200 MHz the two cold corners read −54.5 and −54.9 dBc and **do not clear the target**, so the overall record verdict is FAIL on that scaled check. Residual settling drift (`drift_q_fc`, −1.60…+2.78 fC against the 2.16–3.68 fC charge-pump asymmetry) is of either sign and is the dominant per-corner uncertainty; the first-to-last-window spread reaches 11 dB. **Taken against a dirty working tree** — see its Netlist provenance field. The direct measurement at the binding 200 MHz is a separate, declared-and-unmeasured campaign, `sim/reference-spur-band-top` (**#533**, DR-024); this record is not superseded by it and remains the evidence for its own 150 MHz operating point. Device mismatch is **off** here, so this number is comparable with the derivation's systematic-only −66.6 dBc row rather than with DR-018's −56.6 dBc systematic + statistical stack (DR-024 Decision 4). | `spec/pll.md#reference-spur` (row 7); #145 (part of #127), closed — re-pointed to **#533** by DR-024 | **measured** (5 of 45 corners, at 150 MHz); target **not met** once scaled to the 200 MHz binding point |
| `pfd-deadzone` | `sim/pfd-deadzone/records/20260801-234626-5b333d0.md` (`b5372a13006f`) | Dead-zone-freedom criterion (ratio > 0.5 AND both UP/DN pulse at Δφ=0) — PASS at every evaluated corner. Residual net charge at zero phase error reported per corner (feeds reference-spur derivation). | #9; feeds `spec/pll.md#reference-spur` (row 7, derived) | design-input, measured |
| `pfd-deadzone` (operating-point manifest) | `sim/pfd-deadzone/records/20260916-051356-8cedbba.md` — a **second manifest in the same experiment directory** (`testbench-operating-point/`), not a supersession: same DUT, same `t_offset` reduction, second operating point, and it deliberately does **not** sweep the near ±200 ps pair the parent's dead-zone verdict rests on, so it computes and claims no dead-zone verdict. | 405 points (45 PVT corners × 3 control voltages × 3 phase offsets), all measured, at the closed loop's own f_ref = 12.5 MHz instead of the parent's 25 MHz. **The systematic static phase offset is a strong function of control voltage, an axis `spec/pll.md`'s budget never named**: across DR-001 Decision 2's ratified 0.9–2.4 V window `t_offset` spans 0.0089 … **1.873 ns**, varying by at least 4× at every corner and up to 87× at one (`ff`/27 °C/3.63 V, 10.4×: 0.175 ns at 2.40 V, 0.657 at 1.65, 1.823 at 0.90; `ss`/−40 °C/2.97 V, 87×). At **Vctrl = 0.90 V it exceeds the ratified 1 ns Lock criterion by itself at 36 of 45 corners** (worst 1.873 ns, `ff`/125 °C/3.63 V); at 1.65 V and 2.40 V, at **none**. The reference frequency is nearly irrelevant by comparison — at the cited corner and 1.65 V, halving f_ref moves the term only 0.671 → 0.628 ns (−6.4 %). Cross-validates `supply-sensitivity`: at the two corners where the closed loop settles near 0.98 V, open-loop-at-0.90 V and closed-loop settled phase agree to within 0.09 ns. | #394; DR-012 Decisions 4/4a/4b; corrects the operating-point-free citation of 0.671 ns in `spec/pll.md`'s static-phase-offset budget | **measured** (full 45-corner grid × the whole ratified control window); no verdict claimed (reports a budget term) |
| `output-driver` | `sim/output-driver/records/20260817-100354-0e9cfc9.md` (`5c0a60662ab2`) | First loaded (50 fF `CLK` load) duty-cycle and output-levels/edge-rate measurement in this repository, 90 points (all five MOS bundles × 3 temperatures × 3 supplies × the two band/Vctrl edge extremes). Duty cycle **44.375–50.696 %**, target not met at 7/90 points (6 `fs`-bundle, 1 `ff`-bundle, all at the `lo` edge / nominal-or-above supply) — worst 44.375 % at `fs`/27 °C/3.63 V, 0.625 pp below the 45 % floor. V_OH/V_OL **1.006–1.044·VDD_VCO / −0.040…−0.006·VDD_VCO**, PASS at all 90 points. Overall record verdict FAIL (on the duty-cycle checks only). | `spec/pll.md#output-duty-cycle`, `#output-levels-and-drive` (rows 13/14); #144 (part of #127) | **measured** (90 points); duty-cycle target **not met** at 7/90 points, levels/drive target **met** at every point |
| `pll-top-smoke` | `sim/pll-top-smoke/records/20260802-160926-8456ff3.md` (`7dec4f0210c1`) | Assembled-top-level acceptance gate: **7 of 7 checks PASS** (residual frequency error, static phase error, divide ratio, output frequency vs. N·f_ref, LOCK level, Vctrl window, PFD DN-branch integration guard) at a **single nominal corner** (`typical`/27 °C/3.30 V) by design — this is a connectivity/closed-loop-existence check, not a PVT performance claim; see the record's own justification and `sim/README.md`'s "worked example of an acceptable one-point justification." | #52 (design-input; assembly acceptance gate, not a spec row) | design-input, measured (single corner, by design) |
| `output-range` | `sim/output-range/records/20260819-160843-4e32f91.md` (`9beb97b3cc01`) — the full-grid record, **superseding** the single-corner pilot `20260731-223426-640560e`, and itself superseded **in one row only** by `sim/output-range/records/20260819-190341-70a4128.md` (the `ff`/27 °C/3.30 V `hi` row, where a hand-killed ERROR is replaced by a reproducible CAPPED/stall characterization — still not a PASS; the other 89 rows are untouched and stand as recorded); the `lo`-edge waveform diagnostic `sim/output-range/records/20260801-061907-67d7127.md` (`380369d949c9`) is cited, not superseded (its claim is the waveform diagnosis, not a corner measurement). | Full 45-point PVT grid at both draft band edges, 90 runs, against the `pll_top` DUT (**DUT changed at #159** — not netlist-for-netlist comparable with the two predecessors): **0 of 45 corners reached a sustained in-window PASS at either edge** (83 FAIL, 4 `OUT-OF-WINDOW` where band 7/N=32 cannot reach 200 MHz anywhere inside 0.9–2.7 V Vctrl at 125 °C/2.97 V, 3 ERROR). The 2 µs window is only ~2.5 reference cycles at the `lo` edge, so a `lo` FAIL bounds nothing about eventual lock; PFD DN-branch guard 49 PASS / 0 FAIL / 38 SUSPECT / 3 ERROR. Two ERROR rows are simulator convergence aborts (`sf`/27 °C, `lo`); the third (`ff`/27 °C/3.30 V, `hi`) did not complete in 151 CPU-minutes — #168. | #12; feeds closed-loop output-band coverage (design input, no independent spec row — `spec/pll.md`'s [Output band](#output-band) row 1 is substantiated by `vco-tuning-range`'s open-loop measurement, not by this campaign) | design-input, **measured** (full grid); no lock PASS at either band edge |
| `period-jitter` | `sim/period-jitter/records/20260905-192724-a2ba48f.md` (`c51227a4fd78`) — first record, `typical`/27 °C/3.30 V only; `sim/period-jitter/records/20260906-015602-f9bef9d.md` (`184d49af1f7d`) — adds the four process-corner extremes at the same temperature/supply; `sim/period-jitter/records/20260906-024225-12bccda.md` (`0a177fb30317`) — adds the remaining eight `typical` points, completing the full 3 × 3 temperature × supply plane at nominal process (the first data this campaign has on either of those two axes); `sim/period-jitter/records/20260906-063728-f3c9c23.md` (`ea55286e1112`) — adds the `ff` and `ss` bundles' complete temperature × supply planes (18 points), the first record of this campaign taken against a **clean tree**, and the first with an overall status of **FAIL**; `sim/period-jitter/records/20260906-080511-69b36ef.md` (`6fa41e96fc59`) — closes the `fs` and `sf` bundles' remaining 16 points (every temperature/supply combination not already covered by the process-corner-extremes record), completing the full mandated 45-point matrix; overall **PASS**; `sim/period-jitter/records/20260906-095050-3a8a6ef.md` — supersedes the `ferr` lock-gate verdict of `20260906-063728-f3c9c23` at its two failing points only, re-measured against the wrap-safe gate of #273; 2/2 **PASS** | Deterministic (control-ripple-driven) period/TIE jitter of the locked output, now measured at **all 45 of the mandated PVT corners** — the complete temperature × supply plane (−40/27/125 °C × 2.97/3.30/3.63 V) at every one of the five MOS bundles (`typical`, `ff`, `ss`, `fs`, `sf`). Same operating point throughout as `reference-spur` (f_ref = 25 MHz, N = 6, f_out = 150 MHz, band 6, Icp code 0). **tj_rms 0.0508–0.2691 % of period across all 45** — worst `typical`/−40 °C/3.63 V, best `ss`/125 °C/3.30 V, unchanged by the last 16 points (their own range, 0.0903–0.2472 % RMS at `fs`/`sf`, sits inside those bounds): every one of the 45 is **inside the 1.0 % RMS draft target — PASS against the draft, unratified threshold, at all 45**, worst-case margin 3.7×. The two new bundles also correct a trend the nominal-process plane suggested: **"colder and higher-supply is worse" is not process-independent.** Temperature is monotone at every bundle (hotter is better), but the supply trend's *sign* flips with process — at `ff` jitter falls as supply rises (−40 °C: 0.2484 → 0.2477 → 0.2173 %; 27 °C: 0.2351 → 0.2149 → 0.1994 %), while at `ss` it rises (−40 °C: 0.1420 → 0.1703 → 0.2240 %; 27 °C: 0.0537 → 0.1457 → 0.1732 %). **The FAIL is a guard defect, not a design finding, and was filed as #273**: 2 of the 18 new points (`ff`/27 °C/2.97 V and `ff`/27 °C/3.63 V) fail the manifest's own `ferr` lock gate at 4.00e-2 against a ±1e-3 ceiling, but both loops are locked — `fout` 150.006/149.976 MHz (40/160 ppm of target), `ffb` within 110 ppm of 25 MHz, measured N = 6.000, and jitter inside the range their seven `ff` neighbours produce. The gate's `phi_a`/`phi_b` samples are `trig`/`targ` edge-pair delays, so each is wrapped into one reference period; at those two points `phi_a` ≈ 39.81/39.86 ns (one 25 MHz period minus a sub-200 ps phase error) against `phi_b` ≈ −0.19/−0.13 ns, making the apparent drift exactly one reference period per microsecond — i.e. 0.04. The value reproduced to seven digits in a separately-launched run of the same grid, so it is deterministic, not noise. The record is **not** edited to say otherwise. **#273 is now closed and that FAIL verdict is superseded**: `sim/period-jitter/records/20260906-095050-3a8a6ef.md` re-measures exactly those two points against a wrap-safe gate (the phase difference unwrapped into ±T_ref/2 before it is differentiated, plus a new wrap-free `ferr_fb` = `ffb`/`f_ref` − 1 corroboration check at ±2e-3; the `ferr` gate itself is unchanged at ±1e-3) and both **PASS** — `phi_a`/`phi_b` come back identical to the digit (39.8121/−0.186479 ns and 39.8571/−0.131045 ns), `tj_rms` identical at 0.2351 %/0.1994 %, and `ferr` reads −1.47e-06/−1.19e-05 instead of 4.00e-2. Same simulation, corrected gate: the FAIL was the guard, exactly as diagnosed. Read that record for those two points' lock verdict and this one for everything else it holds. Cross-host reproducibility, which this record was deliberately overlapped to test: `ff` and `ss` at 27 °C/3.30 V were previously measured with ngspice-46 on Linux/x86-64 and re-measured here with ngspice-46 on macOS/arm64 — 0.2072 → 0.2149 % (+3.7 % relative) and 0.1333 → 0.1457 % (+9.3 %), same order and same verdict, not bit-identical. Other guards PASS at every one of the 18 (worst passing |`ferr`| 30.8 ppm, `dn_lvl` ≥ 0.124 against a 0.00089 floor, `dt_int_mean` ≤ 85.3 ps under the 100 ps ceiling). Random/noise-driven jitter is explicitly **not measured** by any of the five records (methodology gap stated per DR-002 Decision 5 — TRANNOISE unsupported on this ngspice build for a free-running oscillator). **DR-020 dispositions that gap** and sharpens it: no ngspice build has a transient device-noise analysis at all, the `.noise` analysis that *does* evaluate the gf180mcu noise PSDs correctly needs a DC operating point an oscillator does not have, and `trnoise()` injects an amplitude the deck author chooses — so the owed item is a validated noise-referral methodology, a simulator that has the analysis natively, or a silicon measurement. **DR-023** locates the missing piece on committed probes (`sim/period-jitter/noise-toolchain-probe/`): the per-device PSDs are available, and what this build lacks is a periodic-steady-state / `pnoise` path to weight them over the oscillation cycle. The item is owed at **#520** (open); DR-020 named #505, which closed the day DR-020 landed. The fifth record (`20260906-080511-69b36ef`) closes the remaining 16 points — the `fs` and `sf` bundles' off-nominal temperature/supply combinations — 16/16, overall **PASS** (no repeat of the #273 lock-gate defect here: worst |`ferr`| 3.65e-05 against the ±1e-3 gate). Temperature stays monotone (hotter is better) at both new bundles too, extending that reading to all five; the supply trend does not resolve into the clean two-valued-by-process-sign picture `ff`/`ss` alone suggested, though — `fs` rises with supply at 27 °C and 125 °C but dips slightly at −40 °C (0.1903 → 0.1876 → 0.2097 %), and `sf` falls gently with supply at −40 °C like `ff` did (0.2472 → 0.2463 → 0.2454 %) but rises at 27 °C and dips at 125 °C. The mandated 45-point PVT matrix for this claim's deterministic half is therefore complete. Compute-cost note, corrected: the earlier records' "a `-j8` full-grid attempt timed out all 45 points" finding was **self-oversubscription, not an inherent per-point cost** — `sim/run_corners.py`'s `-j` is process-level fan-out while this host's ngspice spawns 8 OpenMP threads per process, so `-j8` demanded 64 threads of 8 cores. `sim/harness/omp.py` now budgets those threads; this record's own eight points completed 8/8 in 4015 s wall (`-j4`, 2 threads per worker) against ~200 competing ngspice threads from unrelated concurrent tenants on the same host. | `spec/pll.md#period-jitter` (row 5, deterministic closed-loop half); #13 (part of #127); #273 (the `ferr` gate defect, fixed — the two affected points re-measured and superseded) | **measured** (45 of 45 corners — the full mandated PVT matrix, deterministic component only), PASS against the draft target at all 45; the one record whose overall status is **FAIL** failed on a non-wrap-safe lock gate at 2 locked corners, never on the jitter claim — that gate is now wrap-safe and those two points re-measured **PASS** in the superseding record `20260906-095050-3a8a6ef` (#273); random component **not measured** — methodology gap disclosed |
| `lock-time` | `sim/lock-time/records/20260831-052456-effc505.md` (`bdc1fb3a37d1`) — the full 270-run PVT × N grid, **superseding** the incomplete 7/90-row subset `20260821-081735-96fa64a.md` (per that record's own Supersedes field); `sim/lock-time/records/20260801-162534-2a81927.md` (`334ebbb27d75`) is cited, not superseded (a process/root-cause diagnostic that fixed a timestep-ceiling bug, not a corner measurement); `sim/lock-time/records/20260908-222537-9b7ca4e.md` (`18e05b6f612e`) — #284's single-corner extended-window/waveform investigation of the 131 non-rail-excursion `relock` FAILs, cited not superseded (a waveform diagnostic at one corner, not a corner-count measurement). | Full 45-point PVT grid at N ∈ {4, 16, 64}, both cold-start (0.9 V clamp) and worst-case re-lock (2.7 V clamp) — 270 runs total, against the `pll_top` DUT, using the design's own real `lock_detector` criterion (not an externally-imposed threshold). **22 PASS / 233 FAIL / 15 ERROR of 270** — `cold` reached a sustained in-window PASS on 21/135 rows (15 ERROR); `relock` reached PASS on 1/135 rows (0 ERROR). 135 is rows, not corners: it is the 45-point mandated PVT grid × N ∈ {4,16,64}, the same way the proposal's section-5 row states it. PFD DN-branch integration guard **255 PASS / 0 FAIL / 0 SUSPECT / 15 ERROR** across all rows — wherever the guard resolved, it read PASS, so the FAIL rows are not simulator-timestep artifacts. **Root-cause reading of the 233 FAILs (this report's own breakdown, not restated from the record's own text):** all 21 `cold` PASSes are at N = 64 (0 at N = 4, 0 at N = 16) — `cold`'s PASS rate is 0 % at the two shorter windows and 47 % (21/45) at N = 64's proportionally longest window, consistent with the record's own reading that a `cold` FAIL usually means the transient window ended before `lock_detector` could assert, not a broken loop. `relock` does not show the same recovery: even at N = 64 (the same proportional window length relative to its own condition as `cold` gets relative to its), only 1/45 corners PASS versus `cold`'s 21/45 — a far larger gap than the 2× absolute window-length difference between the two conditions would predict by itself. Of `relock`'s 134 FAILs, 3 (all at the `ff`/−40 °C/3.63 V corner, N = 4/16/64) show `vctrl_final` > 2.7 V, the rail-excursion signature `sim/lock-time/records/20260801-073931-eec269e.md` root-caused as a large-frequency-error PFD/CP acquisition-dynamics anomaly, not a per-corner bug. **The remaining 131 non-rail-excursion `relock` FAILs**: #284's follow-up record (`20260908-222537-9b7ca4e.md`) examined one representative corner (`typical`/−40 °C/2.97 V, N=4) in depth — extending its window the full 2× the harness's own `SIM_WINCAP` mechanism can deliver (40→80 reference periods; `SIM_WINCAP` cannot lengthen a `relock` row past the same 80-reference-period ceiling `cold` already uses, a real constraint that record documents) and reading the waveform: `vctrl` falls fast for ~20–30 reference periods then **plateaus** (a 7 mV band held for 44 reference periods, slope ~170× smaller than the initial acquisition slope and reversed in sign), well short of this same corner's own measured lock point (`cold`/N=64 PASS at `vctrl_final` = 1.561 V in the source record above). An uncommitted supplementary probe found `fb` running ~1.49–1.50× `ref`'s rate, flat to within 0.3 % from partway through the window to its end — the frequency error is not shrinking either. **Reads as option (b): a genuine loop-convergence stall at this corner, not a window-too-short artifact** — flagged to `loop-dynamics` (#10), consistent with (though a corner-dependent, dn-dominant rather than up-dominant, variant of) the mechanism `20260801-073931-eec269e.md` root-caused at its own pilot corner. **This is one corner of the 131, examined deeply and explicitly not generalized to the other 130** (see that record's own Limitations) — the aggregate 131-row question is therefore **still open**, now with one representative, evidenced answer rather than none. **The re-take of this grid is no longer held.** DR-013 Decision 7 held it behind #411's window change, since every verdict here is taken against the design's own `lock_detector`; #411 has landed that window, so the hold is released. A re-take must configure the detector per `spec/pll.md`'s [Lock-detector window trim-code rule](../spec/pll.md#lock-detector-window-trim-code-rule) — the 233 FAILs above were taken against the untrimmed cell and are not comparable point-for-point to a re-take at the rule's codes. | #163; #284; #411 (hold released); `spec/pll.md#lock-time` (row 9) — cold-start acquisition, now measured (not to a clean PASS); `spec/pll.md#lock-detector` — the T1/T2 gap is a *candidate*, not confirmed, explanation for some `cold` FAILs | **measured** (full 270-run grid); 22/270 PASS, 233/270 FAIL (`cold` FAILs read as window-limited, not loop-broken, on DN-guard-PASS rows; of the 131 non-rail-excursion `relock` FAILs, 1 corner is now evidenced as a genuine convergence stall — flagged to #10 — the other 130 remain unattributed by extension, not by re-measurement, see #284), 15/270 ERROR |
| `supply-sensitivity` | `sim/supply-sensitivity/records/20260901-155456-46b92f8.md` (`024486df28d6`) — supersedes `sim/supply-sensitivity/records/20260831-220545-c19e066.md` (`473dcb373ea0`, PR #256/PR #258's 3-corner step/ramp-only check), which itself superseded the full-grid baseline `20260831-133708-35c5a33.md` (`92c02f50a4a2`, #58) and, further back, the original 9-corner `20260801-024935-c4fe724.md` (`f72872845501`, #62) and its re-classification `20260801-114140-f87afc5.md` (`f257dbf0bddc`, #69). **Plus, alongside it and superseding nothing, `sim/supply-sensitivity/records/20260920-180604-0f91a9b.md` (#417)** — a deliberate 2-cell / 6-point subset at the **trimmed** `delaywin_3v3` (DR-014, #411), run to answer DR-013's window-vs-offset crossing question and nothing else. It is **not** this campaign's PVT statement and must not be read as one; `20260901-155456-46b92f8` still is. **Plus, alongside it and superseding nothing, `sim/supply-sensitivity/records/20260925-044237-4ff4f65.md` (#506, behind DR-021)** — a re-examination record in the same form as `20260801-114140-f87afc5` (#69), `20260915-105055-2d6ab99`/`20260915-124108-49f539f` (#384/#389), `20260915-202323-62391c6` (#387, behind DR-011) and `20260916-051708-8cedbba` (#394, behind DR-012): **0 new simulations**, every number arithmetic on `20260901-155456-46b92f8`'s own committed artefacts, reproducible from `sim/supply-sensitivity/testbench/fail_criteria_reexamination.py` and unit-tested in `sim/tests/test_supply_sensitivity_fail_reexamination.py`. It re-reads the three FAILing criteria against the **ratified** spec lines and supersedes nothing. | Full 45-point grid (5 MOS bundles × 3 temperatures × 3 supplies) plus all 3 step/ramp corners, run at the PFD's 100 ps closed-loop timestep bound throughout — this issue's own real, unshortened live run (#257), superseding the mechanism-verification-only #256/#258 record above. **Criterion 1 (frequency vs. supply) FAIL**: 35/45 corners meet the full lock criterion; worst frequency deviation −294 ppm (`typical`/27 °C/3.63 V). 0 corners needed the automatic 36.8 µs settling escalation. The 10 failures split as 2 static-phase-error (routes to the post-#24 charge pump, #9) and 8 `lock_detector` window misses (routes to #11, not a loop failure) — nothing routes to `loop-dynamics` (#10) or `vco-tuning-range` (#8) on criterion 1's account. **Criterion 1b (Vctrl inside DR-001's 0.9–2.4 V window) FAIL**: 4 of 45 corners' ripple peak leaves the window (worst 2.642 V at `ss`/−40 °C/3.63 V). **Criterion 2 (static phase offset, post-#24 CP)**: reported; worst 2.136 ns at `sf`/−40 °C/3.63 V, **2** of 45 corners over the deck's 0.02-of-a-period (1.6 ns) threshold. (This cell previously read "10 of 45 corners over it"; the record's own breakdown attributes 2 to the phase check and the other 8 to the `lock_detector` flag — 10 is the total failure count across all five checks, not the phase count.) **Re-read against the ratified ≤1 ns criterion by `records/20260916-051708-8cedbba.md` (#394, DR-012)**, which supplies the ratified spec line this cell says is missing and changes what the numbers mean: **17** of 45 rows exceed the ratified bound, but 15 of those were sampled with the phase still decaying (the grid's late window sits at 0.69–1.03 τ), so only **2** are settled violations — 1.227 ns at `ff`/27 °C/3.63 V and 1.049 ns at `typical`/−40 °C/3.63 V. Seven rows carry an overall `PASS` while over the ratified bound, because the deck's threshold was 1.6× looser than the spec's; that is fixed for future runs (`ACC_PHI_S`) and the committed verdicts are **not** re-judged. **Criterion 3 (stays locked through a supply step + ramp) FAIL, now with both settling escalations backed by real, full-length transients** (the prior record above verified only the escalation *mechanism* on a truncated deck): the **HIGH-plateau escalation (#253, PR #256)** re-runs all 3 sampled corners (`ff`/125 °C, `ss`/−40 °C, `typical`/27 °C) — every one missed `ferr_hi` at the default 14.4 µs hold (worst 0.04514 at `ss`/−40 °C) and **all 3 resolve to inside the 1e-3 criterion at the escalated 40.1 µs hold** (worst 0.0002851) — **0 of 3 genuine design-margin corners**; all 3 classify `not-locked` (the digital `lock_detector` flag itself does not assert at the escalated hold even though frequency has converged — a lock-range/`#11` finding, not a damping one, the same class as criterion 1's own 8 `lock_detector` misses above). The **END-plateau escalation (#255, PR #258)** then re-runs the same 3 corners' post-ramp hold from `t_rend` + 31.2/34.4/36.8 µs on the HIGH-plateau-escalated profile: `ff`/125 °C and `typical`/27 °C settle inside 1e-3 (settling-budget artifacts), but `ss`/−40 °C does **not** (`ferr_end` 0.001705 at the extended hold, decay ratio 0.809 vs. a 0.0493 single-pole expectation) — **1 genuine design-margin corner, flagged to `loop-dynamics` (#10)** rather than absorbed as a settling artifact. **Criterion 4 (power vs. the draft <5 mW target) PASS**: 0.9863–1.98 mW across the grid (worst `ff`/125 °C/3.63 V); quiescent/dynamic power split (3 corners), e.g. nominal corner 0.4244 mW quiescent / 1.045 mW dynamic. Open-loop VCO supply pushing (cited from `vco-tuning-range`) is *not* re-derived here — this campaign measures the *closed-loop* consequence, a different quantity. **At the trimmed window (#417, `20260920-180604-0f91a9b`, `SIM_PICKS='typical -40 ff 27'` at `KWINTRIM=rule`)**: DR-013 §Consequences' window-vs-offset crossing is now read out of **one** closed-loop transient per cell rather than across two campaigns at two reference frequencies — at `typical`/−40 °C/3.63 V (code 7) the loop settles at **0.8135 ns** and `lock` asserts (window **above** the offset); at `ff`/27 °C/3.63 V (code 11) it settles at **1.233 ns** and `lock` stays at 6.9 nV (window **at-or-below**). Both match what DR-013 inferred, so its Decision 4 verdict is **confirmed**, now on an in-loop measurement. Note the offset at `typical`/−40 °C/3.63 V reads 0.8135 ns here against the 1.049 ns `20260916-051708-8cedbba` settled at — a different DUT (trimmed detector) and this run's own escalated 36.8 µs transient, so the two are not the same measurement and this record does not re-judge that one. The other four of these six points are reported for context only: criterion 3 is **NOT MEASURED** in that run (its `typical`/27 °C corner is outside the picks, so it never got a calibrated warm start) and the quiescent/dynamic power split is empty. **Re-read against the ratified lines by `records/20260925-044237-4ff4f65.md` (#506, DR-021), which changes what two of the three FAILs are evidence of without changing a verdict.** (1) **Criterion 1 has no frequency content**: 0 of 45 corners missed either frequency check — worst |fdev| 294 ppm of 1000 (3.4× headroom), worst |ferr| 2.255e-4 of 1e-3 (4.4×). Joining DR-012's committed `settled` grading to the verdict column shows its two classes sit on the wrong corners: neither `FAIL:phi` corner was settled when sampled (`ff`/125 °C/3.63 V moved 0.722 ns and `sf`/−40 °C/3.63 V 0.605 ns across the run's own 3.2 µs window), while both **settled** violations of the ratified ≤ 1 ns bound — 1.227 ns at `ff`/27 °C/3.63 V and 1.049 ns at `typical`/−40 °C/3.63 V — carry `FAIL:lock`, i.e. sit inside the 8 the record routed to #11 (closed). So this cell's "routes to #9 … routes to #11" split above is the right *mechanism* attached to the wrong *rows*, and the whole class is DR-012's finding read at two nodes. (2) **Criterion 1b's window check grades a superseded figure, and the ratified budget was never graded at all**: the 4 failing points leave DR-001 Decision 2's *predicted* 0.9–2.4 V window, which **DR-003 Decision 5** superseded with a measured 0.9–2.7 V on 504 curves — against that window **0 of 45 points leave it**, tightest margin **+53 mV** at `ss`/−40 °C/3.63 V. `spec/pll.md` row 12's normative **Budget 2** (≤ 0.6 V of the window consumed by a 2.97–3.63 V excursion) is a different check and is **missed at 9 of the 15 (bundle, temperature) cells**, worst **0.846 V** at `ss`/−40 °C (1.41×, 47 % of the 1.8 V window); read over the ±0.33 V excursion the budget's own derivation prices, 0 of 15 exceed it. The consumption is not anomalous — it matches what each cell's selected band requires, from #8's committed open-loop f(Vctrl, vdd) table, to within **5.7 mV at every cell** (1.9 mV at the worst). (3) **Criterion 3's one `under-damped` corner recovers as a straight line, not an exponential**: the retained END-plateau waveform has the control node slewing at a near-constant −20.3 mV/µs for 18.75 µs after the ramp at `ss`/−40 °C, a line fitting those samples with 33 % less residual in volts than a single exponential (20.54 vs 30.75 mV rms; 21 % less at `typical`/27 °C; at `ff`/125 °C the 4-bin segment discriminates nothing). Section 3c's `decay_ratio` vs `exp(−Δt/τ)` discriminant assumes the model the waveform contradicts, and the model-free damping test does not separate that corner from the two called `settles` (17.2 mV of overshoot on its own 20.0 mV of ripple, against 19.5 and 22.5 mV). What separates it is recovery *length*, which ranks exactly as the cells' Budget-2 consumption (18.75/9.75/2.25 µs against 0.846/0.698/0.558 V); at the extended hold's end it still had 13.11 ns of phase closing at 1.705e-3, i.e. **≈7.7 µs more** — a hold ~8 µs short, the same shape and size as DR-011's independent 2.8–8.2 µs high-plateau bracket. | `spec/pll.md#supply-sensitivity` (row 12, pushing figure cited from `vco-tuning-range`, not from this campaign); `spec/pll.md#power` (`vdd_ref` domain current — this record's "core" column); `spec/pll.md#lock-detector` (row 16 item (b), via #417); DR-013 §Consequences follow-up; #58; #253; #255; #257; #417; **DR-021** (what each FAIL is evidence of, and its owner); `spec/pll.md#supply-sensitivity` Budget 2 (now measured and missed, per DR-021); DR-003 Decision 5 (the measured 0.9–2.7 V control window); #506; **#525** (Budget 2's excursion, and the deck's mis-graded criterion 1b); **#511**/**#399** (the static-phase finding); **#405** (the post-ramp settling measurement) | **measured** (full 45-point grid + all 3 step/ramp corners, both settling escalations run to completion); criteria 1/1b/2/3 **FAIL** (the committed verdicts stand; `sim/` is append-only), criterion 4 **PASS**. **Re-classified, not re-judged, by DR-021/#506**: criterion 1 is DR-012's static-phase finding with 0 frequency content (owners **#511**, **#399**, **#437**); criterion 1b's window FAIL is against a window DR-003 superseded, while the ratified Budget 2 is missed at 9/15 cells (owner **#525**); criterion 3's END-plateau miss is a slew-limited recovery ~8 µs short of settled rather than the under-damped loop routed to the now-closed #10 (owner **#405**, scope item 3). Separately, **measured at the trimmed window over a declared 2-cell subset** (#417) — DR-013's crossing **confirmed** at both cells it names; a trimmed-window **full-grid** re-run is **#437** |
| `vco-bias-current` | `sim/vco-bias-current/records/20260911-172350-da0bb1e.md` (`84800baac0da`) | Constant-gm reference current of the VCO bias generator with the bias resistors as **specified** (`ppolyf_u_3k`, 3000 Ω/sq) against the same generator with them as the pre-#381 **layout drew** them (unmarked `ppolyf_u`, 350 Ω/sq), 234 points (13 corner bundles × 3 temperatures × 3 supplies × 2 band codes). Measured class ratio at the drawn RCG geometry **6.78–9.72×** (8.30× typical/27 °C — the "8.6×" figure #381 states, measured). At `spec/pll.md`'s own 100 MHz reference point (band 5, Vctrl 1.8 V) the as-specified bias generator draws **68.7–195.6 µA**; the as-drawn-350 arm draws **970–2163 µA**, i.e. **7.4–20.4×** more and above the ratified **318 µA** `vdd_vco` domain current at *every* point of the grid on its own, before the ring. Per-stage ring starving current (replica `MNT`) 19.1–48.4 µA as specified vs. 199.5–397.6 µA as drawn. Overall PASS. **The counterfactual arm is a diagnostic, never a design alternative.** | `spec/pll.md#power` (row 10, the 318 µA `vdd_vco` figure, cross-checked not re-derived); DR-009; #381 | **measured** (full 13-bundle grid); confirms the layout fix and quantifies the defect it removed |
| `vco-tuning-range` | `sim/vco-tuning-range/records/20260804-211600-f599a65.md` (`9b2ab6ddf83e`) — the **supply-pushing/jitter** sub-claim (see caveat: this directory now carries 4 live sub-claims from 4 manifests/decks). The **output-band/Kvco** sub-claim's current record is `sim/vco-tuning-range/records/20260731-175947-0a12e6c.md` (`6a0c337af4e1`). The **band-select mirror mismatch** sub-claim (new, #146) is `sim/vco-tuning-range/records/20260817-143524-0e9cfc9.md`, a raw `sim/lib/simenv.sh` deck (not a `tb.json` manifest — see that record for the harness-ordering finding making a `params` override of `sw_stat_mismatch` silently ineffective). | **This campaign runs a 63-point PVT grid** — 7 bundles × 3 temperatures × 3 supplies — which is a *superset* of the 45-point mandated grid: it adds `all-slow` and `all-fast`, the two combined bundles that skew every device family together (passives included) rather than MOS alone. Every worst case below is therefore a worst case over a wider corner universe than a mandated-grid campaign's, and is binding at a corner the mandated grid does not contain. Output band: floor 6.449 MHz (`all-fast`/125 °C/2.97 V, 36 % below the 10 MHz line), ceiling 247.8 MHz (`all-slow`/−40 °C/3.63 V, 24 % above the 200 MHz line), 0 non-monotonic curves of 504, worst adjacent-band overlap 27 %. Kvco worst 115.8 MHz/V (`all-fast`/27 °C/2.97 V, band 6) under the band-selection rule. Supply pushing: worst −54.5 %/V (`ss`/−40 °C, band 5) static; ripple sensitivity worst **2.51 % RMS period jitter** at 100 mV pp `vdd_vco` ripple (`all-slow`/−40 °C/2.97 V, band 5) — **the number `spec/pll.md`'s summary table row 5 cites**, and the basis for row 5's conditional-on-ripple-budget framing. Overall PASS on all three sub-decks. **Band-select mirror mismatch** (device-level Monte Carlo, nominal PVT, N=25/band at the two cascade extremes): band 7 (full cascade, highest bias current) disperses 15.4 % of mean (\|mean\|+3σ), comfortably under the 26.8 % worst-adjacent-overlap headroom the systematic sweep measured; band 0 (always-on legs only, lowest bias current) disperses **38.8 %**, which EXCEEDS that headroom — an honest, not-yet-formally-closed finding (order-of-magnitude proxy comparison only, not a matching-corner combined verdict) that random mismatch alone could plausibly erode band 0's coverage margin at the low end of the band plan. No numeric budget exists for this term in `design/README.md`. | `spec/pll.md#output-band`, `#kvco` (rows 1/17); `#period-jitter` (row 5, sensitivity half); `#supply-sensitivity` (row 12, pushing figure); #8, #146 | **measured**; band-select mismatch sub-claim: **measured, with an open finding at band 0** |
| `cp-compliance` | Two current sub-claims: **DC bench** — `sim/cp-compliance/records/20260801-190821-734f483.md` (`c60f07e9b9e7`); **switching-timing bench** — `sim/cp-compliance/records/20260802-061841-c24ee3a.md` (`2e973c1f6c50`). See caveat below on a third, near-duplicate switching record. | DC bench: both current sources stay in saturation across the ~0.9–2.4 V Vctrl window at every corner; UP/DN mismatch and the 2-bit Icp trim range measured (unit-leg currents 1.68–7.21 µA across codes/corners, nominal ≈5.2 µA). Switching bench: effective UP/DN pulse-width skew (`wskew`) measured per (corner, Vctrl); worst-corner asymmetry 3.68 fC (`fs`/125 °C/3.63 V, Vctrl 0.9 V) — **feeds `spec/pll.md#reference-spur`'s derivation directly.** Overall PASS on both benches. | #9; feeds `spec/pll.md#reference-spur` (row 7, derived), the Icp trim-code rule, and `mc-cp-mismatch`'s systematic baseline | design-input, measured |
| `harness-selftest` | `sim/harness-selftest/records/20260731-151931-58c52d9.md` (`b054e15b269c`) | `sim/harness`'s own acceptance testbench (real devices, no design claim) — proves the harness resolves the PDK, sweeps corners, and writes a `sim/README.md`-conformant record end to end. Overall PASS. **This row said "45-point PVT grid" until 2026-09-25; the record's own corner field reads "63 point full-factorial grid"** across 7 bundles — the five MOS ones plus `res_ff` and `res_ss`, deliberately, since exercising a passive axis is part of what the harness is being proved to do. A stated grid that the cited record contradicts is exactly the failure this table's coverage check now looks for, and it was hiding behind the check's own allowance of the mandated size. | n/a — harness self-verification, not a design or spec claim | n/a (tooling) |

## Verification-owed cross-check

`spec/pll.md`'s own "Verification owed" table (line 879) lists what the
specification asserts that `sim/` does not yet substantiate. Every row of
that table is disclosed above; this section restates the correspondence so a
reader auditing gap-coverage does not have to cross-reference by hand:

| `spec/pll.md`'s owed item | Disclosed above under | Still open as of this report |
|---|---|---|
| Period jitter — closed-loop / random (noise-driven) jitter | `period-jitter` (deterministic component, 45 corners — full mandated matrix); `vco-tuning-range` (open-loop sensitivity substitute) | **partially** — `sim/period-jitter/` now has **six** records and the deterministic component is measured at all 45 of the mandated PVT corners (0.0508–0.2691 % RMS, PASS against the draft target at every point). Those 45 are a **union of five records, no one of which covers the grid**, which is why this row now names them instead of pointing at the directory: `sim/period-jitter/records/20260905-192724-a2ba48f.md` (1 run, `typical`/27 °C/3.30 V), `sim/period-jitter/records/20260906-015602-f9bef9d.md` (4, the other MOS bundles at that same temperature and supply), `sim/period-jitter/records/20260906-024225-12bccda.md` (8, completing the `typical` temperature × supply plane), `sim/period-jitter/records/20260906-063728-f3c9c23.md` (18, the `ff` and `ss` planes) and `sim/period-jitter/records/20260906-080511-69b36ef.md` (16, the `fs` and `sf` planes, closing the matrix) — 47 runs at 45 distinct points, the two repeats being `ff`/27 °C/3.30 V and `ss`/27 °C/3.30 V, measured once in the second record and again inside the fourth's planes. The sixth record, `sim/period-jitter/records/20260906-095050-3a8a6ef.md`, re-measures 2 points of the fourth (`ff`/27 °C at 2.97 V and at 3.63 V) against #273's wrap-safe lock gate and adds no new corner — so the record count moved and the coverage did not. Random/noise-driven jitter is explicitly **not measured** by any of them (disclosed methodology gap, DR-002 Decision 5) and remains wholly owed. **DR-020 records its disposition**: no analysis this flow offers produces it (ngspice has no transient device-noise model; `.noise` needs a DC operating point a free-running oscillator does not have; `trnoise()` injects a chosen amplitude), so what is owed is a methodology before a number. **DR-023** narrows that reason — the device noise PSDs are available; the missing piece is a periodic-steady-state / `pnoise` path to carry them over the oscillation cycle — and re-points the owner to **#520** (open), neither the closed #13 nor #505, which DR-020 named and which closed the day DR-020 landed. The ≤ 1.0 % RMS target is unchanged — satisfied in simulation for its deterministic half only, with the 0.50 % RMS the row's ripple derivation leaves for the random half an unverified budget (DR-023 Decision 3) |
| Period jitter — band sweep at non-nominal temp/supply | `period-jitter`, `period-jitter-band-top`, `vco-tuning-range` | **partially** — no longer wholly owed: `sim/period-jitter/records/20260906-024225-12bccda.md` closes the complete temperature × supply plane (−40/27/125 °C × 2.97/3.30/3.63 V) at nominal process, so both axes now have real closed-loop data (0.1111–0.2691 % RMS, worst cold-and-high-supply). The **output-band** axis, which no record of `period-jitter` varies — every point of it is at band 6 / 150 MHz — is now a declared campaign of its own, `sim/period-jitter-band-top`: the same measurement, the same reduction code, at the 200 MHz top of `spec/pll.md#output-band`, with its full 45-point grid declared and every point's operating point derived from the committed VCO record `20260804-162735-72883fb` under `spec/pll.md`'s normative band-selection rule (`band_and_vstart_from_vco_record.py --check`). That VCO record runs the **63-point** superset grid (7 bundles), but the derivation reads only its five mandated MOS bundles, so what is declared here is the mandated grid, not the wider one it is derived from. Deriving it surfaced a finding the 150 MHz campaign could not have: **at 200 MHz the rule does not select one band code for the grid** — band 6 at 34 of the 45 points, band 7 at the other 11 (the cold and/or high-supply points where band 6's own curve tops out below 200 MHz) — where at 150 MHz one static code covers all 45, which is why `reference-spur` and `period-jitter` could hold theirs fixed. Local Kvco at the selected points spans 74.7–117.0 MHz/V, all inside `spec/pll.md` row 17's 150 MHz/V bound, but the loop gain the one fixed filter sees therefore varies 1.6× across that grid against 1.05× across `period-jitter`'s. **Still owed**: every measured point of `period-jitter-band-top` — the 150 MHz temperature/supply-plane grid this row used to track is now complete (`sim/period-jitter/records/20260906-080511-69b36ef.md` closes the `fs` and `sf` bundles' remaining 16 points, so all 45 of the mandated PVT corners are measured at 150 MHz); `period-jitter-band-top`'s manifest, deck and derivation are committed and self-checking, so what remains there is compute, not design — and since #496 that compute has a route: `sim/run_corners.py period-jitter-band-top --backend batch` shapes the 45 points as one off-host job each, preserving per-point isolation, timeouts and provenance (see `sim/harness/README.md` §`--backend`). What is owed is now an authorized run of that path, not a missing mechanism; zero of the 45 points are measured either way. The cited open-loop ripple-jitter number is likewise one operating point, not a full grid |
| Reference spur — the mandated 45 PVT points at the binding 200 MHz | `reference-spur` (the measurement, at 150 MHz), `reference-spur-band-top` (the binding-point campaign, declared and unmeasured); `cp-compliance`, `mc-cp-mismatch`, `pfd-deadzone` (the derivation's inputs) | **partially** — the closed-loop measurement exists at 5 spanning corners at 150 MHz (#145, closed), and its two cold corners do not clear −55 dBc once scaled to 200 MHz (−54.5 and −54.9 dBc). What is owed is **restated by DR-024 Decision 3**: the mandated 45 points at the binding 200 MHz, not 40 more points at 150 MHz that would still have to be scaled by +2.50 dB before they could be read against the line. That sweep is now a declared campaign of its own, `sim/reference-spur-band-top` — the same deck, spectral reduction, solver settings and spectral windows as `reference-spur`, at N = 8 / f_out = 200 MHz, with every point's band code and release voltage derived from the committed VCO record `20260804-162735-72883fb` under the normative band-selection rule (`band_and_vstart_from_vco_record.py --check`, the same generator `period-jitter-band-top` uses, so the two 200 MHz campaigns are configured identically point for point). That VCO record runs the **63-point** superset grid (7 bundles), but the derivation reads only its five mandated MOS bundles, so what is declared here is the mandated 45, not the wider grid it is derived from. The same 34/11 band split applies here and is why this could not be five more corners of `reference-spur`, whose band bits are static in `params`. **Still owed**: every one of the 45 points — manifest, deck and derivation are committed and self-checking, so what remains is compute, not design, and the route is `sim/run_corners.py reference-spur-band-top --backend batch`; an authorized run of it is **#533**, blocked on the same fleet-credential fault as #499/#503. **Not owed here**: the statistical half of the spur — both closed-loop campaigns run with device mismatch off, so a measurement is comparable with the derivation's systematic-only **−66.6 dBc** row, not with DR-018's −56.6 dBc stack (DR-024 Decision 4); a closed-loop Monte-Carlo spur campaign is unowned |
| Lock time — cold-start acquisition incl. cycle slipping | `lock-time` | **partially** — see the `lock-time` row above: the full 270-run closed-loop grid now exists (#163) and measures cold-start directly (22 PASS / 233 FAIL / 15 ERROR of 270), so this is no longer "only small-signal settling"; most `cold` FAILs read as window-limited rather than a broken loop. Of the 131 non-rail-excursion `relock` FAILs (excluding the 3 known rail-excursion rows), #284's follow-up investigation (`20260908-222537-9b7ca4e.md`) found one representative corner genuinely stalled — not window-limited — and flagged it to `loop-dynamics` (#10); the other 130 remain unattributed by extension of that one corner's evidence, not by independent re-measurement — still open |
| Reference input — thresholds/edge-rate sweep, numeric reference-jitter limit | `reference-input-contract` (declared, no record) | **yes — wholly owed, but no longer unowned.** A campaign directory now covers the first half: `sim/reference-input-contract` holds the DUT at `sim/pfd-deadzone`'s operating point (`pfd_cp`, 25 MHz, Vctrl = 1.65 V, Icp code b1 b0 = 10) and sweeps the `REF` **waveform** to each boundary `spec/pll.md#reference-input` states — 0.2·VDD – 0.8·VDD levels, 5.00 ns 10–90 % edges, 30 % and 70 % duty, and one variant at all three at once — against the ideal full-rail 200 ps/50 % pulse every other testbench here drives. Its graded quantity is the per-corner shift of the reference-path set delay `d_ref` (reference mid-rail crossing → the instant the PFD acts on it), held to the 1 ns static-phase bound of `spec/pll.md`'s Lock criterion, with the duty row's rising-edge-only argument stated falsifiably at ≤ 1 % of `d_ref`. The deck measures its own applied edge rate and duty and gates them, so a run proves it drove what it claims. **Every one of the 288 declared points is owed** (45 mandated PVT corners × 6 waveform variants, plus an 18-point detector-gain slice at `ss`/125 °C/2.97 V, `ff`/−40 °C/3.63 V and `typical`/27 °C/3.30 V) — the same declared-and-unmeasured state `period-jitter-band-top` is in, and for the same reason: what remains is compute, not mechanism or design. The second half, a numeric reference-jitter limit, needs the closed-loop noise bench and is **unowned** — sequenced behind the noise methodology at #505, and deliberately not re-pointed at #13, which is itself closed; DR-019 re-points both halves off closed issue #12 and restates the source-quality exclusion with its boundary and its owner |
| Power — measured `vdd_ref` domain current, closed-loop total | `supply-sensitivity` (`vdd_ref` == its "core (PFD+CP+LD)" column, and the closed-loop total) | **no longer** — `supply-sensitivity`'s full-grid record (#58) measures both: total power 0.9865–1.98 mW (PASS against the draft <5 mW target) and the `vdd_ref`/core-domain current at every corner, plus the quiescent/dynamic split. `spec/pll.md#power` still states `vdd_ref` as budget-only and has not been updated to cite this measurement (`spec/` changes are out of scope for this report) |
| Output duty cycle — no measurement exists | `output-driver` | **partially** — the measurement now exists at 90 points (#144); the design does not meet its own 45 % floor at 7/90 points (`fs` bundle, `lo` edge, nominal-or-above supply), and post-extraction re-run / the on-die divider load are not yet added |
| Output levels and drive — loaded-output swing/edge-rate | `output-driver` | **partially** — the measurement now exists at 90 points and PASSES at every point (#144); post-extraction re-run / the on-die divider load are not yet added |
| Lock detector — T1′/T2′ window band, T4/T5 below 25 MHz | `lock-detector`, `lock-window-trim`, `lock-window-sizing` | **partially — T1′ and T2′ are now met; T4/T5 are not characterized.** The route there took three steps, each on evidence the previous one could not produce: DR-010's geometric re-size (W = 9.5 µm, #393) met T1′ but the in-situ re-characterization found **T2′ exceeded by 1.0–2.0 %** at `ss`/125 °C/2.97 V, a finding `lock-window-sizing`'s idealized delay-chain-only stimulus could not show; DR-013 (#401) established that **no** sizing of that topology could meet the band (spread 1.928× at W = 8 µm, 1.906× at W = 26 µm) and set a ≤ 1.65× spread target; DR-014 (#407) picked a fixed test-set trim code over a bias-referenced delay; and **#411** built it and re-characterized under it — `lock-window-trim` (1872 points) for the code map and code table, `lock-detector` (205 points) for the in-situ verdict: edges [1.14, 1.16) ns and [1.78, 1.80) ns, spread **1.53–1.58×**, both targets met with 10–16 % of margin. **What is not covered**: T4 (deassert latency ≤ 1 reference period) and T5 (no chatter) below 25 MHz — the detector has only ever been run at f_ref = 25 MHz and its assert hold-off is an absolute time, so at 1 MHz it is of order one reference period; a 0.02 ns edge refinement at `ff`/−40 °C/3.63 V and `ss`/−40 °C/3.63 V, where only the coarse ladder ran; `supply-sensitivity` at the new window across the **full** grid (#417 measured it at the two cells DR-013 names — crossing **confirmed** in one loop — and left the other 43 points at the untrimmed cell); and extraction plus mismatch, including trim-array DNL (#18) |
| Lock criterion — the loop's settled static phase vs. the ratified ≤1 ns | `supply-sensitivity`; `pfd-deadzone` (operating-point manifest) | **partially, and the finding moved** (#394, DR-012). `sim/supply-sensitivity/records/20260916-051708-8cedbba.md` re-reads all 45 rows of the committed steady grid: the loop misses the ratified bound in undisturbed steady state at **2** corners on settled evidence — **1.227 ns** at `ff`/27 °C/3.63 V and 1.049 ns at `typical`/−40 °C/3.63 V, each stationary to ≈11 ps across the late window and cross-checked from the PFD's UP/DN pulse-width difference to <2.3 %. All **8** corners whose own `lock_detector` refused to assert are over the bound (8 of 8), so those `FAIL:lock` rows are not purely a #11 window question. The **1.796 ns** previously entered here for `ff`/125 °C/3.63 V is withdrawn: that run stopped at ≈1.3 τ with the phase still falling (2.518 ns one window earlier), so it is an upper bound on a tail and that corner's settled value is **unmeasured**. The `pfd-deadzone` half of the reconciliation is now measured (operating-point manifest row above) and answers "is ≤1 ns reachable, and where": **not at the bottom of the ratified control window** — the systematic term alone exceeds the whole criterion at 36 of 45 corners at Vctrl = 0.90 V — **and yes over the rest of it**, none at 1.65 V or 2.40 V. The axis is the control voltage, not the PVT corner. Still owed: the settled phase at the **15** unsettled over-bound corners (needs the 36.8 µs escalation, ≈10 h of ngspice per corner) and any measurement at an (f_ref, N, trim-code) cell other than 12.5 MHz / N=8 / b1b0=10 |
| Lock time — re-lock after a mid-operation supply excursion | `supply-sensitivity` | **no** — only a lower bound is measured: `lock` had not re-asserted 34.4 µs (3.70 τ) after a +10 % step at 2 of 3 sampled corners. `sim/supply-sensitivity/records/20260915-202323-62391c6.md` (`b2a13f8a4245`) brackets the recovery at 37.2…42.6 µs from committed data, but that is a two-sample extrapolation contradicted in sense by `46b92f8` §3c's `under-damped` result at one of the same corners. DR-011 records it as a stated limitation; #395 is the campaign that would measure it |
| Area — everything except the loop filter | `loop-dynamics` (loop filter only) | **yes** — no floorplan exists (#17), so no other block's drawn area is measured |
| Kvco / Output band — Monte Carlo band-select mismatch, post-extraction re-run | `vco-tuning-range` (schematic-level only) | **yes** — every VCO number cited above is schematic-level (`sim/README.md`'s "Netlist provenance" convention); no post-extraction re-run exists (#18) |
| Multiplication ratio — post-extraction retiming margin at N=64/200 MHz | `divider-ratio`, `divider-ratio-chain` | **yes** — the cited 6.1 % margin is schematic-level; #18 owes the post-layout re-take |

No item from `spec/pll.md`'s Verification-owed table is silently omitted
here; every row above traces to an explicit "still open" note in this
report, in the campaign table, or both.

## Caveats and notes

- **`divider-ratio`'s spec citation is stale relative to the migration, but
  the numbers agree.** `spec/pll.md#multiplication-ratio`'s own "Evidence"
  line still cites the pre-harness `sim/divider-ratio/` records (`#41`
  migrated the campaign onto `sim/harness` by splitting it into
  `divider-ratio-dff`/`-cell`/`-chain`, per `sim/README.md`'s "A one-directory
  experiment can outgrow one `sim/harness` manifest"). As of this report,
  the migrated `divider-ratio-chain` record reproduces the pre-migration
  retiming-margin number exactly (3.03e-10 s / 6.1 %, same binding corner),
  so the two do not disagree — but `spec/pll.md` has not been updated to
  cite the migrated sibling directories. Not fixed here (`spec/` changes are
  out of this issue's scope per its own Guardrails); noted so it does not
  read as an oversight of this report.
- **`cp-compliance` carries an apparent unreconciled duplicate.** Two
  records — `20260802-033728-c24ee3a` and `20260802-061841-c24ee3a` — both
  supersede the same predecessor (`20260731-194124-afa338c`) for the
  switching-timing bench, without either superseding the other. Their
  headline numbers agree closely (same measurement, same grid, run ~2.5
  hours apart). This report cites the later of the two
  (`20260802-061841-c24ee3a`) as current, consistent with the "latest by
  timestamp" rule used throughout, but the lack of an explicit
  Supersedes link between the pair is a minor bookkeeping gap in `sim/`
  itself, not something this report can fix without editing an append-only
  record.
- **Multi-record campaigns.** `cp-compliance` (DC + switching benches) and
  `vco-tuning-range` (output-band/Kvco, stage-count comparison, and
  supply-pushing/jitter, three manifests sharing one experiment directory
  per `sim/README.md`) each carry more than one simultaneously-current
  record — this report cites both (or all) in each such case, per
  `sim/README.md`'s own citation convention ("cited, not superseded").
  `output-range`, `supply-sensitivity`, and `lock-time` were once in a
  third category — a latest-by-date record that was a narrow diagnostic or
  re-classification rather than the broad measurement — until their
  full-grid records (`20260819-160843-4e32f91`, #159;
  `20260831-133708-35c5a33`, #58; `20260831-052456-effc505`, #163) landed:
  each campaign's latest record is now the broad measurement, and the
  earlier diagnostic (`output-range`'s waveform diagnostic;
  `supply-sensitivity`'s 400 ps-bound re-classification; `lock-time`'s
  timestep-ceiling-bug diagnostic `20260801-162534-2a81927`) is the one
  cited alongside it.
- **`pll-top-smoke` and `harness-selftest` are single-corner or non-design
  campaigns by design**, not incomplete PVT sweeps — see their own rows
  above and `sim/README.md`'s "worked example of an acceptable one-point
  justification" for why a single corner is a complete answer to the
  question those two campaigns ask.
- **klayout-tools #309** — the general aggregation-tool issue this repo's
  interim report does not block on (per #147's own Scope item 2) — closed
  on 2026-08-09, and the tool it asked for shipped as `klt signoff`, which
  this repository runs as `signoff/run-signoff.sh` against
  `signoff/block-manifest.json`. That grades the T1 checklist and the
  evidence hashes the manifest pins; it does not generate or grade this
  report's summary table, which is still hand-maintained. Earlier revisions
  of this bullet said #309 "remains open", which stopped being true about
  six weeks before anyone re-read it.

## Maintenance

This report is **hand-maintained**, not generated. It reflects the tree as
of the commit that introduced it; every citation above should be re-checked
against the current tree before being relied on for anything beyond a
quick orientation, and definitely before being cited outside this
repository. `sim/lib/check-characterization-coverage.sh` (added alongside
this report) fails CI if a campaign directory under `sim/*/records/` exists
on disk without a corresponding entry in this report's summary table, so
the two cannot silently drift apart in the "a whole new campaign appeared
and nobody added it here" direction — it does **not** detect a stale
headline number, a hash that no longer matches its record, or a new record
inside an already-listed campaign. Catching those needs either disciplined
re-review at PR time (this report's own diff should be part of any PR that
adds a `sim/*/records/*.md` file with a materially different headline) or
a generator for this report. The general aggregation tool klayout-tools
#309 asked for has landed as `klt signoff` (see "Caveats and notes" above), but it
grades `signoff/`'s manifest, not this report, so today disciplined
re-review is the only one of the two that exists.

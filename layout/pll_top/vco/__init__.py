"""Real transistor-level layout for the VCO block (issue #293).

``design/vco.sch`` / ``vco_stage.sch`` / ``vco_bias.sch`` are the schematics
this layout implements; ``layout/floorplan/PLL-FLOORPLAN.md`` sections 1 and
5 are the isolation/placement plan it follows. See ``ring.py`` for the
top-level ``build()`` entry point.
"""

# PLL floorplan block-placement skeleton — DRC run (issue #17)

Generated by `python3 -c "from floorplan import skeleton; skeleton.build(...)"`
(see `layout/floorplan/skeleton.py`) followed by
`python3 layout/run_pv.py drc pll_floorplan_skeleton.gds --top
pll_floorplan_skeleton`. See `layout/floorplan/PLL-FLOORPLAN.md` for the
floorplan this skeleton implements and what running it through DRC does and
does not prove.

## What this is, and is not

This is **not** a signoff DRC-clean claim about the PLL's physical design.
`design/` has no top-level layout view for `vco`, `pfd_cp`, or
`divider_chain`/`lock_detector` yet, so there is no real device geometry for
those blocks to be DRC-clean *or* dirty. What this skeleton is: one boundary
rectangle per block (plus two real, as-drawn sub-geometries — the loop-filter
C1/C2 array from DR-006 and the VCO's committed 22 pF decap from `vco.sch`),
all drawn on GDS layer 0/datatype 0 (`DIEAREA`), positioned per
`PLL-FLOORPLAN.md`'s isolation and supply-routing plan.

Layer (0, 0) carries **no rule** in this PDK's DRC deck — confirmed by
grepping every rule file under
`$PDK_ROOT/libs.tech/klayout/drc/rule_decks/*.drc` for a reference to it
(none). So this DRC run is clean **by construction** (there is no rule that
could fire against this layer), not because the floorplan's real geometry
has been checked. The value of running it anyway is exercising #16's DRC
flow against a multi-shape, multi-block layout rather than only the single
trivial cell `layout/evidence/inv-tb-proof/` proves the flow on — a different
kind of coverage (does the flow handle a layout with several disjoint
boundary regions and text labels?), not a stronger DRC claim.

## Provenance

| | |
|---|---|
| Generated | 2026-09-08T18:22 UTC |
| Invoked as | `python3 layout/run_pv.py drc ... --run-dir /tmp/pll-floorplan/drc`, with `LAYOUT_PV_PYTHON` pointed at a local venv (`klayout` + `docopt`) created per `layout/README.md`'s Prerequisites |
| PDK | `gf180mcuD`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (volare) |
| KLayout (application, deck runner) | `KLayout 0.28.16` |
| DRC deck | `<pdk>/libs.tech/klayout/drc/run_drc.py`, table `main`, `--variant=D` (the same invocation `layout/harness/drc.py` always uses) |

## Result

| Check | Expected | Got | Verdict |
|---|---|---|---|
| `pll_floorplan_skeleton` DRC, table `main` | clean (no rule keys off layer 0/0) | `Klayout DRC run is clean. GDS has no DRC violations.` | **PASS** |

Skeleton bounding-box extent (block rectangles only, excluding the VCO guard
ring's outward margin): **148,157 µm² (0.148157 mm²)** — a sanity cross-check
against `PLL-FLOORPLAN.md`'s area-budget table, not the same number as that
table's ROM footprint estimates (this bbox includes the inter-domain spacing
channels the budget's top-level overhead multiplier accounts for
separately, and does not include the VCO guard ring's own outward extent).
Still under the < 0.15 mm² draft target, but only just.

**Regenerated for issue #293's assembled VCO block** (was 117,975 µm² when
this record was first written, with `VCO_CORE` a 140 × 100 µm ROM
placeholder). `VCO_CORE` is now the real, wired, DRC-clean VCO block's own
guard-ring box — 294.78 × 148.18 µm, 43,680 µm² — which is a 2.6–4.0×
overrun against `PLL-FLOORPLAN.md` §5's 0.011–0.017 mm² ROM row for the
whole VCO, and takes this bounding box from ≈21 % headroom against the draft
target to ≈1.2 %. §5's own "fail-loud condition" asks for exactly this to be
stated rather than rounded away; the full accounting is in
[`../vco-layout/PROOF-block.md`](../vco-layout/PROOF-block.md) and
`layout/floorplan/skeleton.py`'s own module docstring.

## Artifacts

| Path | What it is |
|---|---|
| `pll_floorplan_skeleton.gds` | the block-placement skeleton (one rectangle + text label per block/sub-block, all on layer 0/0) |
| `drc-clean/pll_floorplan_skeleton_main.lyrdb` | KLayout DRC report database (empty violations) |
| `drc-clean/drc.stdout.log` | captured `run_drc.py` stdout+stderr |

Regenerate via `layout/floorplan/skeleton.py` + `layout/run_pv.py drc` (see
`PLL-FLOORPLAN.md` § "GDS skeleton"); do not hand-edit any file under this
directory.
